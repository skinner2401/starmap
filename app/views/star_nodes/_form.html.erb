<%= form_with(model: star_node) do |form| %>
  <% if star_node.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(star_node.errors.count, "error") %> prohibited this star_node from being saved:</h2>

      <ul>
        <% star_node.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :north_star_id %>
    <%= form.number_field :north_star_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
